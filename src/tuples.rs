//Tuple of length 3
pub fn run() {
    let tupple = ('E', 6i32, true);
    // Use tupple indexing and show the values
    // of the elements in the tupple
    println!("{}, {}, {}", tupple.0, tupple.1, tupple.2); // -> Dot notation
}
